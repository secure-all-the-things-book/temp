package com.example.devicecli;

import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
//@ImportAutoConfiguration(exclude = SpringShellAutoConfiguration.class)
class CliClientApplicationTests {
/*

    @Test
    void go(@Autowired LoginCommands command) throws Exception {
        var deviceAuthRequest = command.deviceCodeGranter.authorize();
        IO.println("🔗 Authenticate at: " + deviceAuthRequest.verificationUri());

        command.token = deviceAuthRequest.accessToken().get();
        System.out.println(command.message());
    }
*/

}